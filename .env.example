# ── API auth ──────────────────────────────────────────────────────────────────
# Set a strong random secret here. All /v1/* requests must send X-API-Key: <value>.
# Leave empty to disable auth (local dev only — never leave empty in production).
API_KEY=

# ── Claude backend ────────────────────────────────────────────────────────────
# Use an Anthropic API key, or set USE_CLAUDE_SUBSCRIPTION=true to authenticate
# via your Claude Pro / Claude Code session (no separate billing card needed).
ANTHROPIC_API_KEY=sk-ant-...
USE_CLAUDE_SUBSCRIPTION=false

# ── GitHub Copilot / GitHub Models backend ────────────────────────────────────
# A GitHub Personal Access Token with the "models:read" scope gives access to
# the GitHub Models API (OpenAI-compatible endpoint) included in your Copilot plan.
# Generate at: https://github.com/settings/tokens
GITHUB_TOKEN=github_pat_...

# ── Ollama ────────────────────────────────────────────────────────────────────
# Default: local Ollama server. Change if you run Ollama on a different host.
OLLAMA_BASE_URL=http://localhost:11434

# ── Budget limits (optional overrides — defaults come from routing.yaml) ──────
# CLAUDE_MONTHLY_BUDGET_USD=20.0
# COPILOT_MONTHLY_PREMIUM_REQUESTS=300

# ── Database ──────────────────────────────────────────────────────────────────
DB_PATH=data/autopilot.db

# ── Observability ─────────────────────────────────────────────────────────────
LOG_LEVEL=INFO
# POST a JSON alert to this URL when Claude spend crosses BUDGET_ALERT_THRESHOLD_USD.
# Leave empty to disable. Works with Slack incoming webhooks, ntfy.sh, etc.
BUDGET_ALERT_WEBHOOK_URL=
BUDGET_ALERT_THRESHOLD_USD=15.0
